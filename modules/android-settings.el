;;; lang/org/autoload/org.el -*- lexical-binding: t; -*-

(provide 'android-settings)

;; undefine gui element
;;   (create issues in termux)
(defun scroll-bar-mode(x))
(defun tool-bar-mode(x))
(defun set-fringe-mode(x))
